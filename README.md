Mes prompts pour l'ia :

cc, je voudrais faire un jeu en p5.js. L'idée est de faire le jeu du snake qui mange un truc et qui se rallonge à chaque fois qu'il mange. Je vais faire le jeu avec une grille car c'est le plus logique à faire (et je ne sais pas si on peut faire autrement ?). Voici mes spécifications. Ne génère pas le code final. Aide-moi à coder étape par étape en m'expliquant chaque fonction p5.js dont j'ai besoin. Commence par la section Canvas.


Comment je peux stocker les positions de chaque carré du serpent pour qu'ils se suivent sans se séparer? tu peux m'expliquer comment fonctionnent les tableaux (arrays) et la fonction push() en p5.js EN DETAIL STPPP ?


Ma nourriture apparaît des fois entre deux cases. comment ke peux forcer p5.js à choisir une position qui tombe EXACTEMENT sur mon carré ? et la fonction floor() peut m'aider ?


Mon jeu est collé en haut à gauche. comment j'utilise cnv.position() pour le mettre au centre dans la page? et c'est bein ça pour le style : 
  cnv.style('border', '4px solid #ff0096'); 
  cnv.style('box-shadow', '0 0 30px #ff0096'); 
  cnv.style('border-radius', '8px');  ?


j'arrive pas à faire grandir le serpent. Il bouge mais garde la même taille (max 5 carrés). Comment je dis au programme de ne pas supprimer le dernier morceau de la queue seulement quand il mange un carré ?
